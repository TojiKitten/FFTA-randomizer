// Idea for Filterable Enums
// Example: var swords = fftaData.items.filter(item => item.displayName in SWORDS);
// Could also fill out using item ID's for easier use when we don't have the display ID
enum SWORDS {
  SHORTSWORD = "Shortsword",
}

enum FEMALEONLY {
  CACHUSHA = "Cachusha",
  BARETTE = "Barette",
  RIBBON = "Ribbon",
  MINERVAPLATE = "Minerva Plate",
  RUBBERSUIT = "Rubber Suit",
}
